test:
    pre:
        - make install
        - make check

    override:
        - make test

notify:
  webhooks:
    # A list of hook hashes, containing the url field
    # gitter hook
    - url: https://webhooks.gitter.im/e/9ef1e17d1cd4299abd5f
